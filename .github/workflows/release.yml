name: Release Workflow
env:
  CONFIG_DIRECTORY: "./terraform"
  REPOSITORY: ${{github.github_actions_repository}}

on:
  push:
    branches:
      - main

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - run: echo ${{env.REPOSITORY}}
    - run: echo ${{github.GITHUB_ACTION_REPOSITORY}}
#    - uses: ${{env.REPOSITORY}}/.github/workflows/build.yml
 #     with:
  #      runs-on: "ubuntu-latest"
   # 
    #- uses: ${{env.REPOSITORY}}/.github/workflows/terraform-plan.yml
    #- uses: ${{env.REPOSITORY}}/.github/workflows/terraform-apply.yml

  
